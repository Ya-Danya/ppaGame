# PostgreSQL container
POSTGRES_DB=paperfx
POSTGRES_USER=paperfx
POSTGRES_PASSWORD=paperfx
# host port (use 5433 to avoid conflicts with local Postgres)
POSTGRES_PORT=5433

# Variables for the Java server (server runs on host)
DB_URL=jdbc:postgresql://127.0.0.1:5433/paperfx
DB_USER=paperfx
DB_PASS=paperfx
